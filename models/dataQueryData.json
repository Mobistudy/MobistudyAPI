{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://mobistudy.org/models/dataQueryData.json",
  "title": "Data query data",
  "description": "Complete data of the data query task, which retrieves data from HealthKit and Google Fit.",
  "type": "object",
  "properties": {
    "dataType": {
      "type": "string"
    },
    "samples": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "endDate": {
            "type": "string"
          },
          "unit": {
            "type": "string"
          }
        }
      }
    }
  },
  "required": [
    "dataType",
    "samples"
  ]
}
